<div class='formField'>
  <label for={{id}} class='fieldLabel'>{{label}}</label>
  <input
    id={{id}}
    type={{type}}
    name={{name}}
    placeholder='{{placeholder}}'
    class='fieldInput'
  />
  {{#if error}}
    <p class='fieldError'>{{error}}</p>
  {{/if}}
</div>